# План переработки МТС по issue #42

## Цель

Пересобрать основания МТС так, чтобы актуальная документация стала короче, чище и не смешивала:

- формальную нотацию смыслов;
- шаблонный поиск;
- ачисла и абиты;
- асеть как остенсивную модель;
- исторические версии аксиом.

## Критерий готовности

1. README показывает статус переработки по issue #42.
2. Актуальная теория начинается с новой рамки, а не со старой таблицы А0–А16.
3. Формальная нотация определяет `{}` как шаблонный поиск без фильтра.
4. Квадратные скобки закреплены за ачислами.
5. Новая аксиома акорня записана как `∞ : {} = {} ⟼ {}`.
6. Повторные `{}` внутри одного исполнения шаблона явно связываются с одной связью-кандидатом.
7. Старые тексты явно воспринимаются как исторические или исследовательские.
8. Код парсеров и конвертеров не переписывается до стабилизации новой нотации.

## Этап 0. Зафиксировать новое направление

- [x] Создать `docs/theory/Основания МТС.md`.
- [x] Обновить `README.md`.
- [x] Обновить `docs/theory/Метатеория связей.md`.
- [x] Обновить `docs/theory/Шаблон аксиом МТС.md`.
- [x] Добавить регрессионный тест для требований issue #42.

## Этап 1. Переписать ядро аксиом

- [x] Создать `docs/theory/Новая система аксиом МТС.md`.
- [x] Зафиксировать рабочее ядро:

```text
∞ : {} = {} ⟼ {}
♂{} : ♂{} = ♂{} ⟼ {}
{}♀ : {}♀ = {} ⟼ {}♀
{[} : ∞♀
{]} : ♂∞
{⟼} : {[} ⟼ {]}
```

- [ ] Нормализовать запись независимых шаблонных поисков.
- [x] Добавить минимальное рабочее правило исполнения шаблона.
- [ ] Полностью формализовать исполнение шаблона.
- [ ] Решить статус `{↛}`, `0`, `1` и инверсии.

## Этап 2. Переписать спецификации

- [x] Обновить `docs/specs/Формальная нотация МТС.md`.
- [x] Создать `docs/specs/Слои нотации МТС.md`.
- [x] Создать `docs/specs/Шаблонный поиск МТС.md`.
- [x] Создать `docs/specs/Ачисла и сериализация.md`.
- [ ] После стабилизации грамматики обновить парсеры.

## Этап 3. Историческая политика

- [x] Добавить явное предупреждение для архивных материалов.
- [x] Перенести исторический анализ скобок из активной теории в архив.
- [ ] По мере дальнейшей работы помечать старые FAQ и research-файлы, где `{}` или `[]` трактуются по-старому.
- [ ] Перенести устаревшие чистовики из актуальных разделов, если они больше мешают чтению.

## Этап 4. Код и тесты

Код менять рано, потому что новая нотация ещё не стабилизирована. Текущая задача кода:

- сохранить работоспособность конвертеров;
- не расширять старую грамматику как будто она финальная;
- добавить тесты, которые фиксируют новую документационную рамку.
- усилить тесты на повторные `{}`, статус исторических документов и отсутствие старой скобочной рамки в активной теории.

## Регистр открытых вопросов

- [x] Создать `docs/research/Открытые вопросы новой аксиоматики МТС.md`.

## Открытые вопросы

1. Что такое пустой результат поиска?
2. Как обозначать пустой пучок связей, если `{}` уже означает поиск без фильтра?
3. Как определить знаки `{` и `}`?
4. Как записывать два независимых шаблонных поиска в одной формуле?
5. Как отличить поиск связей от действия над найденными связями?
6. Как согласовать новую нотацию с существующими конвертерами ачисел?
7. Как описывать асеть как структуру, восстановленную из ачисла?
