# План стабилизации рабочей МТС

## Цель

Собрать текущую рабочую МТС так, чтобы система аксиом, формальная нотация, тест формул и структура документации проверяли друг друга.

## Критерий готовности

1. README является точкой входа в текущую МТС.
2. `docs/theory/Шаблон аксиом МТС.md` задаёт нормативный критерий готовности аксиомы.
3. `docs/theory/Система аксиом МТС.md` построена по этому шаблону.
4. Каждый символ, используемый в формулах ядра, имеет аксиому или явный служебный статус.
5. `tests/mtc_formulas.mtc` содержит только чистые формулы МТС и комментарии.
6. Регрессионные тесты запрещают служебный DSL в `.mtc`.
7. `{}` отделено от пустого множества, пустого результата поиска, несвязи и `0`.
8. `{s}` отделено от `{}`.
9. Последовательная запись форм читается как связь с левой группировкой.
10. `[]` остаются только в слое ачисел и сериализации.

## Рабочее ядро

```text
∞ : {} = {} ⟼ {}
∞ = ∞ ⟼ ∞

({}) = {}

{}{} : {} ⟼ {}
{}{} = {} ⟼ {}

{}{}{} : ({} ⟼ {}) ⟼ {}
{}{}{} = {} ⟼ {} ⟼ {}
{}{}{} != {} ⟼ ({} ⟼ {})

♂{} : ♂{} = ♂{} ⟼ {}
♂∞ = ♂∞ ⟼ ∞

{}♀ : {}♀ = {} ⟼ {}♀
∞♀ = ∞ ⟼ ∞♀

{[} : ∞♀
{]} : ♂∞

{[}{]} = {[} ⟼ {]}
{⟼} : {[}{]}
{⟼} = {[} ⟼ {]}
```

## Выполнено

- [x] Обновить README как карту текущей МТС.
- [x] Свести активную теорию к четырём документам в `docs/theory/`.
- [x] Свести активные спецификации к четырём документам в `docs/specs/`.
- [x] Добавить регистр открытых вопросов в `docs/research/`.
- [x] Удалить старые theory/research-файлы из активной документации.
- [x] Вернуть канонический `tests/mtc_formulas.mtc`.
- [x] Удалить временные `issue42` и `legacy` fixtures из `tests/`.
- [x] Усилить `tests/test_issue42_docs.py` под чистые формулы.
- [x] Добавить аксиомы группировки, последовательности и прямоассоциативности.

## Открытые вопросы

1. Что такое пустой результат поиска?
2. Как записывать независимые шаблонные поиски вне последовательностной записи?
3. Являются ли `{` и `}` внутренними знаками МТС или служебными знаками формальной нотации?
4. Как полностью формализовать исполнение шаблона?
5. Как записывать `{↛}`, `0`, `1` и инверсию в рабочей системе?
6. Какой parser/test-runner должен проверять будущую исполняемую грамматику `tests/mtc_formulas.mtc`?
7. Как описывать асеть как структуру, восстановленную из ачисла?
